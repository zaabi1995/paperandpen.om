---
import { useTranslations, localizePath, type Locale } from '@/i18n';
import Icon from './Icon.astro';

interface Props {
  locale: Locale;
  eyebrow?: string;
  title: string;
  subtitle: string;
  bullets?: string[];
  primaryCta?: { href: string; label: string };
  secondaryCta?: { href: string; label: string };
}
const { locale, eyebrow, title, subtitle, bullets = [], primaryCta, secondaryCta } = Astro.props;
const t = useTranslations(locale);
const lp = (p: string) => localizePath(p, locale);
const primary = primaryCta ?? { href: '/signup', label: t('hero.cta') };
---
<section class="relative overflow-hidden bg-mesh-dark text-cream-50 pt-24 pb-20 lg:pt-28 lg:pb-24">
  <div class="absolute inset-0 bg-grid pointer-events-none"></div>

  <div class="relative max-w-4xl mx-auto px-6 lg:px-10 text-center">
    {eyebrow && (
      <div class="inline-flex items-center gap-2 glass text-copper-300 text-xs font-semibold px-3.5 py-1.5 rounded-full mb-6 tracking-widest uppercase">
        <span class="w-1.5 h-1.5 rounded-full bg-copper-400 animate-pulse"></span>
        {eyebrow}
      </div>
    )}
    <h1 class="font-display text-4xl sm:text-5xl lg:text-6xl font-bold text-cream-50 leading-[1.08] mb-6" set:html={title}></h1>
    <p class="text-cream-100/70 text-lg lg:text-xl leading-relaxed max-w-2xl mx-auto mb-9">{subtitle}</p>

    <div class="flex flex-col sm:flex-row gap-3 justify-center">
      <a href={lp(primary.href)} class="btn-glow">{primary.label}
        <svg class="w-4 h-4 rtl:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 12h14m-7-7l7 7-7 7"/></svg>
      </a>
      {secondaryCta && <a href={lp(secondaryCta.href)} class="inline-flex items-center justify-center px-7 py-3.5 glass text-cream-50 font-semibold rounded-xl hover:bg-white/10 transition-all">{secondaryCta.label}</a>}
    </div>

    {bullets.length > 0 && (
      <ul class="flex flex-wrap gap-x-6 gap-y-2 justify-center mt-8">
        {bullets.map((b) => (
          <li class="flex items-center gap-2 text-sm text-cream-100/70">
            <span class="text-emerald-400"><Icon name="check" class="w-4 h-4" /></span>{b}
          </li>
        ))}
      </ul>
    )}
    <p class="mt-6 text-xs text-cream-100/40 tracking-wide">{t('hero.trialNote')}</p>
  </div>
</section>
